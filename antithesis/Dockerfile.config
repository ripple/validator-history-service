# Dockerfile for Antithesis config image
# This image contains the docker-compose.yaml and other configuration files
# required by Antithesis to orchestrate the test environment

FROM scratch

# Copy docker-compose configuration
COPY docker-compose.yaml /docker-compose.yaml

# Copy any additional configuration files if needed
# COPY config/ /config/

